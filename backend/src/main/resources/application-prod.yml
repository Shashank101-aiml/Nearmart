spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/buildit_prod
    username: ${DB_USER:postgres}
    password: ${DB_PASS:secret}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
